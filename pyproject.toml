[tool.black]
line-length = 100
target-version = ['py310']
include = '\.pyi?$'
exclude = '''
/(
    \.git
  | \.venv
  | build
  | dist
  | venv
)/
'''

[tool.isort]
profile = "black"
[tool.mypy]
plugins = "pytest_mypy_plugins.mypy_plugin"
