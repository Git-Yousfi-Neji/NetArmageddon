[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
addopts = "--cov=netarmageddon --cov-report=term-missing"
testpaths = ["tests"]
python_files = "test_*.py"